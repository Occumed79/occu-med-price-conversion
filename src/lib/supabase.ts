// Stable import path for the Supabase browser client.
// The client itself lives in src/integrations/supabase/client.ts and is
// configured from Vite env vars (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY).
export { supabase } from "@/integrations/supabase/client";
